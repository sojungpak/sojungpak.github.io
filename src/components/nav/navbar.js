import React from 'react';
import NavLinks from './navLinks';
import Switch from './switch';

const Navbar = (props) => {
  return (
    <div>
      <NavLinks />
      <Switch />
    </div>
  );
};

export default Navbar;
